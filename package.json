{
  "name": "superficies",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --experimental-specifier-resolution=node --test src/**/*.test.ts",
    "check": "tsc --noEmit",
    "build": "npm run test && npm run check && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
