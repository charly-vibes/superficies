Implemented superficies-4zg.6 deterministic export workspace: added pure export artifact metadata/listing helpers, enriched full/minimum/token exports with current live dimensions, accessibility target, anti-defaults, and shared token-derived JSON; render now generates tabs from artifact definitions. Verified npm test, npm run check, npm run build, and openspec validate --specs --strict.
